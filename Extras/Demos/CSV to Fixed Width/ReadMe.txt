Converts CSV data to fixed width. Run against "CSV.txt".

In Linux, to run this demo from a terminal, do the following:

   - Open a terminal.
   - Change to this folder.
   - Run this shell command:

     cat "CSV.txt" | pcl "call './CSV to Fixed Width.pip'"
