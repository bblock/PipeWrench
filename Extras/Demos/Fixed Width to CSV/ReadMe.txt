Converts fixed width data to CSV. Run against "Fixed Width.txt".

In Linux, to run this demo from a terminal, do the following:

   - Open a terminal.
   - Change to this folder.
   - Run this shell command:

     cat "Fixed Width.txt" | pcl "call './Fixed Width to CSV.pip'"
